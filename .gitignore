/.phpunit.cache
/node_modules
# Playwright, installed ad hoc by the container smoke test and never a
# project dependency. Rooted /node_modules above does not cover it, and an
# unstaged `git add -A` would otherwise commit a browser engine.
/.github/scripts/node_modules
/public/build
/public/fonts-manifest.dev.json
# item/docs-site (issue #27): generated by
# .github/scripts/build-docs-site.mjs, uploaded to Pages by
# .github/workflows/pages.yml -- never committed.
/docs/.site
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/.agents
/.amp
/.claude
/.cursor
/.factory
/.grok
/.junie
/.kiro
/.pi
/.mcp.json
/AGENTS.md
# CLAUDE.md is tracked on purpose: it carries the project's
# conventions and the testing traps this codebase actually hit,
# which every contributor and agent needs.
/boost.json
/opencode.json
/opencode.jsonc

# Python bytecode from the .github/scripts guards (py_compile, pytest, a bare run)
__pycache__/
*.pyc
