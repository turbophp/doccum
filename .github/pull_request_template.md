<!--
Keep this short. A PR nobody can review in two minutes gets reviewed in zero.
-->

## Summary

<!-- One or two sentences: what changed and why. -->

## Backlog item (ledger id)

<!-- The docs/ledger/ledger.jsonld id this PR closes or advances, e.g. LDG-042.
     "none" if this genuinely has no ledger entry (docs typo, CI-only change, etc). -->

## Changes

<!-- Bullet list. Call out anything that touches a seam from CLAUDE.md
     (DocumentStorage, ProcessRunner, SearchIndex, Search, DirectoryAccess,
     DoccumServiceProvider) or either authorisation layer (Spatie permission,
     directory_access). -->

-

## Verification

<!-- How CI proves this: which job/matrix leg exercises it, and what you ran
     locally that CI doesn't cover yet (e.g. `docker compose up -d --build`). -->

## Risks

<!-- What could this break, and what would show it: a rollback, a migration,
     a change to a provider seam, nothing in particular. -->
